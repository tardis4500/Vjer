build:
  steps:
  - build_args:
      CICD_IMAGE_REPO: '{var:CICD_IMAGE_REPO}'
    dockerfile: Dockerfile.dotnet
    image: bart-dotnet
    name: Build Dotnet Docker image
    type: docker
project:
  product: BaRT
  version: 11.2.1
release:
  steps:
  - image: bart-dotnet
    name: Release Dotnet Docker Image
    type: docker
  - ignore: true
    type: gitlab
  - ignore: true
    type: tag_source
schema: 1
