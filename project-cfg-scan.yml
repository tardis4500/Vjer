build:
  steps:
  - build_args:
      CICD_IMAGE_REPO: '{var:CICD_IMAGE_REPO}'
    dockerfile: Dockerfile.scan
    image: bart-scan
    name: Build Scan Docker image
    type: docker
project:
  product: BaRT
  version: 11.2.1
release:
  steps:
  - image: bart-scan
    name: Release Scan Docker Image
    type: docker
  - ignore: true
    type: gitlab
  - ignore: true
    type: tag_source
schema: 1
