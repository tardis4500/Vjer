build:
  steps:
  - build_args:
      CICD_IMAGE_REPO: '{var:CICD_IMAGE_REPO}'
    dockerfile: Dockerfile.sencha
    image: bart-sencha
    name: Build Sencha Docker image
    type: docker
project:
  product: BaRT
  version: 11.2.1
release:
  steps:
  - image: bart-sencha
    name: Release Sencha Docker Image
    type: docker
  - ignore: true
    type: gitlab
  - ignore: true
    type: tag_source
schema: 1
